# Production analytics values embedded into the static marketing build.
TINYANALYTICS_ENDPOINT=https://analytics.beddybytes.com
TINYANALYTICS_WRITE_TOKEN=REPLACE_BEFORE_ENCRYPTING

# AWS destination for the static marketing site.
AWS_REGION=REPLACE_BEFORE_ENCRYPTING
BUCKET=REPLACE_BEFORE_ENCRYPTING
DISTRIBUTION_ID=REPLACE_BEFORE_ENCRYPTING
