# BeddyBytes UI Prototypes

Use this folder for layout explorations before migrating `frontend` or `marketing`.

The components in this folder are intentionally not exported from `@beddybytes/ui`.
They are review artifacts for app and marketing layouts.
